# Build
build/bin/
build/frontend/

# Frontend
frontend/node_modules/
frontend/dist/

# Wails auto-generated
wailsjs/

# Database
*.db
*.db-shm
*.db-wal

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Go
vendor/

# Docs (local planning only — anchored to v2/docs, not nested screens/docs)
/docs/

# Secrets
.env
.env.local
.ai
# AgentDB (claude-flow runtime files)
agentdb.rvf
agentdb.rvf.lock

# libghostty embed (Wave 5) — ghostty cloned externally, built locally
# Setup: git clone https://github.com/ghostty-org/ghostty third_party/ghostty && make libghostty
third_party/
# Built binary at repo root from `go build ./cmd/phantom-libghostty-demo/`.
# Anchored so the source dir `cmd/phantom-libghostty-demo/` stays tracked.
/phantom-libghostty-demo
