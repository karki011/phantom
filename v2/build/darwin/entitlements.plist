<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Required for the WebKit JS engine (V8/JavaScriptCore) under hardened runtime. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<!-- Wails embeds a Go binary; library validation must be relaxed. -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- App makes outbound HTTP calls (GitHub, Anthropic, etc.). -->
	<key>com.apple.security.network.client</key>
	<true/>
	<!-- Allow loading the local dev server / any embedded http listener. -->
	<key>com.apple.security.network.server</key>
	<true/>
</dict>
</plist>
